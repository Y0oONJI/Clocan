# Component Architecture Documentation

## 🏗️ 컴포넌트 구조 현황 및 분석

이 문서는 Closet Canvas 프로젝트의 컴포넌트 아키텍처를 분석하고 개선 방향을 제시합니다.

---

## 📋 목차

1. [컴포넌트 계층 구조](#컴포넌트-계층-구조)
2. [의존성 그래프](#의존성-그래프)
3. [컴포넌트 분류 및 역할](#컴포넌트-분류-및-역할)
4. [현황 분석](#현황-분석)
5. [개선 제안](#개선-제안)

---

## 1. 컴포넌트 계층 구조

### 📊 전체 계층 차트

```
┌─────────────────────────────────────────────────────────────────────┐
│                          APPLICATION LAYER                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌────────────────┐    ┌────────────────┐    ┌────────────────┐   │
│  │  RootLayout    │    │   HomePage     │    │  StyleQuizPage │   │
│  │  (layout.tsx)  │    │  (app/page)    │    │ (/style-quiz)  │   │
│  │                │    │                │    │                │   │
│  │  - Toaster     │    │  - Hero        │    │  - StyleQuiz   │   │
│  │  - Fonts       │    │  - Features    │    │                │   │
│  │  - Metadata    │    │  - CTA         │    │                │   │
│  └────────────────┘    └────────────────┘    └────────────────┘   │
│                              │                       │              │
│                              │                       │              │
└──────────────────────────────┼───────────────────────┼──────────────┘
                               │                       │
                               ▼                       ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         FEATURE LAYER                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                        ┌──────────────────┐                         │
│                        │   StyleQuiz      │                         │
│                        │  (Main Feature)  │                         │
│                        │                  │                         │
│                        │  States:         │                         │
│                        │  - step          │                         │
│                        │  - selections    │                         │
│                        │                  │                         │
│                        │  Logic:          │                         │
│                        │  - toggleSelect  │                         │
│                        │  - handleNext    │                         │
│                        │  - validation    │                         │
│                        └─────────┬────────┘                         │
│                                  │                                  │
│           ┌──────────────────────┼──────────────────────┐           │
│           │                      │                      │           │
│           ▼                      ▼                      ▼           │
│     ┌──────────┐          ┌──────────┐          ┌──────────┐       │
│     │  Step 1  │          │  Step 2  │          │  Step 3  │       │
│     │  Styles  │          │  Colors  │          │ Inspire  │       │
│     └──────────┘          └──────────┘          └──────────┘       │
│                                                                     │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               │ uses
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          UI COMPONENTS LAYER                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │  Button  │  │   Card   │  │ Progress │  │  Badge   │           │
│  │          │  │          │  │          │  │          │           │
│  │ Variants │  │ - Header │  │ Radix UI │  │ Variants │           │
│  │ - default│  │ - Content│  │ based    │  │ - second │           │
│  │ - outline│  │ - Footer │  │          │  │ - destru │           │
│  │ - ghost  │  │ - Title  │  │          │  │          │           │
│  │ - ...    │  │ - Desc   │  │          │  │          │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
│                                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │  Toast   │  │ Toaster  │  │  Alert   │  │  Dialog  │           │
│  │          │  │          │  │          │  │          │           │
│  │ Radix UI │  │ useToast │  │  Shadcn  │  │  Radix   │           │
│  │ based    │  │ hook     │  │          │  │          │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
│                                                                     │
│  + 29 other shadcn/ui components (currently unused)                │
│                                                                     │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               │ uses
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      UTILITIES & HOOKS LAYER                        │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐         │
│  │     cn()     │    │  useToast    │    │ useIsMobile  │         │
│  │              │    │              │    │              │         │
│  │ clsx +       │    │ State mgmt   │    │ Breakpoint   │         │
│  │ tailwind-    │    │ Reducer      │    │ detection    │         │
│  │ merge        │    │ pattern      │    │ 768px        │         │
│  └──────────────┘    └──────────────┘    └──────────────┘         │
│                                                                     │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               │ uses
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          DATA LAYER                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │              quiz-data.ts (NEW)                      │          │
│  │                                                      │          │
│  │  export const QUIZ_STYLES: StyleOption[]            │          │
│  │  export const QUIZ_COLORS: ColorPalette[]           │          │
│  │  export const QUIZ_INSPIRATION_IMAGES: string[]     │          │
│  │                                                      │          │
│  │  export type StyleOption = { id, name, imageId }    │          │
│  │  export type ColorPalette = { id, name, palette }   │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
│  ┌──────────────────────────────────────────────────────┐          │
│  │           placeholder-images.ts                      │          │
│  │                                                      │          │
│  │  export const PlaceHolderImages: ImagePlaceholder[] │          │
│  │  - Loads from placeholder-images.json               │          │
│  └──────────────────────────────────────────────────────┘          │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 2. 의존성 그래프

### 🔗 주요 의존성 관계

```
HomePage (/)
  └─> Button, Card
      └─> cn (utils)

StyleQuizPage (/style-quiz)
  └─> StyleQuiz
      ├─> Button, Card, Progress
      │   └─> cn, Radix UI
      ├─> QUIZ_STYLES, QUIZ_COLORS, QUIZ_INSPIRATION_IMAGES
      │   └─> quiz-data.ts (types included)
      ├─> PlaceHolderImages
      │   └─> placeholder-images.json
      └─> useRouter (next/navigation)

ResultPage (/style-quiz/result)
  ├─> Button, Card, Badge
  │   └─> cn
  ├─> QUIZ_STYLES, QUIZ_COLORS (for data lookup)
  └─> useSearchParams, useRouter

RootLayout (app/layout.tsx)
  └─> Toaster
      └─> Toast, useToast
          └─> Radix UI Toast primitives
```

### 📦 외부 라이브러리 의존성

```
React Ecosystem:
  - react, react-dom (v19+)
  - next.js (App Router)

UI Framework:
  - @radix-ui/react-* (primitives)
  - tailwindcss (styling)
  - class-variance-authority (variants)
  - clsx + tailwind-merge (utils)

Icons:
  - lucide-react

AI Integration:
  - genkit
  - @genkit-ai/google-genai
  - zod (schema validation)
```

---

## 3. 컴포넌트 분류 및 역할

### 📂 파일 구조별 분류

```
src/
├── app/                          [Application Layer - 4 files]
│   ├── layout.tsx               → Root layout + Toaster
│   ├── page.tsx                 → Landing page
│   └── style-quiz/
│       ├── page.tsx             → Quiz page wrapper
│       └── result/
│           └── page.tsx         → Result page with AI analysis
│
├── components/                   [Feature + UI Layer - 35 files]
│   ├── style-quiz.tsx           → Main feature component (229 lines)
│   └── ui/                      → 34 reusable UI components
│       ├── button.tsx           ✅ USED
│       ├── card.tsx             ✅ USED
│       ├── progress.tsx         ✅ USED
│       ├── toast.tsx            ✅ USED
│       ├── toaster.tsx          ✅ USED
│       ├── badge.tsx            ✅ USED (result page)
│       └── [28 others]          ⚠️  NOT USED
│
├── hooks/                        [Hooks Layer - 2 files]
│   ├── use-toast.ts             ✅ USED (by Toaster)
│   └── use-mobile.tsx           ⚠️  NOT USED
│
├── lib/                          [Utilities Layer - 3 files]
│   ├── utils.ts                 ✅ USED (cn function)
│   ├── placeholder-images.ts    ✅ USED
│   └── placeholder-images.json  ✅ USED
│
├── data/                         [Data Layer - 1 file] 🆕
│   └── quiz-data.ts             ✅ USED (exported constants + types)
│
└── ai/                           [AI Layer - 4 files]
    ├── genkit.ts                ⚠️  DEFINED but not used in UI
    ├── dev.ts                   ⚠️  Dev entry point
    └── flows/
        ├── find-similar-clothing-items.ts      ⚠️  Ready but not called
        └── outfit-suggestion-from-wishlist.ts  ⚠️  Ready but not called
```

### 📊 사용 현황 통계

| 카테고리 | 총 파일 수 | 사용 중 | 미사용 | 사용률 |
|---------|-----------|--------|--------|--------|
| **App Routes** | 4 | 4 | 0 | 100% |
| **Feature Components** | 1 | 1 | 0 | 100% |
| **UI Components** | 34 | 6 | 28 | 17.6% |
| **Hooks** | 2 | 1 | 1 | 50% |
| **Utilities** | 3 | 3 | 0 | 100% |
| **Data** | 1 | 1 | 0 | 100% |
| **AI Flows** | 4 | 0 | 4 | 0% |
| **전체** | **49** | **16** | **33** | **32.7%** |

---

## 4. 현황 분석

### ✅ 강점 (Strengths)

1. **명확한 레이어 분리**
   - App, Feature, UI, Data, AI 레이어가 명확히 구분됨
   - 각 레이어의 책임이 잘 정의됨

2. **일관된 디자인 시스템**
   - shadcn/ui 기반 컴포넌트로 통일성 확보
   - Radix UI primitives로 접근성 보장

3. **타입 안정성**
   - TypeScript 전면 사용
   - Data layer에 타입 정의 포함 (`StyleOption`, `ColorPalette`)

4. **재사용 가능한 컴포넌트**
   - Button, Card 등 atomic 컴포넌트가 잘 분리됨
   - variant 패턴으로 유연한 스타일링

5. **데이터-UI 분리 개선** 🆕
   - 하드코딩된 데이터를 `quiz-data.ts`로 분리
   - 유지보수성 대폭 향상

### ⚠️ 약점 (Weaknesses)

1. **낮은 컴포넌트 사용률**
   - UI 컴포넌트 34개 중 28개 미사용 (82.4%)
   - 사전에 생성된 컴포넌트가 실제 요구사항과 불일치

2. **StyleQuiz 컴포넌트 비대화**
   - 229줄의 단일 컴포넌트
   - 여러 책임 혼재 (상태관리, UI 렌더링, 데이터 처리)
   - 테스트 및 유지보수 어려움

3. **AI 기능 미통합**
   - AI flows가 정의되어 있으나 UI에서 미사용
   - Result 페이지가 시뮬레이션만 수행

4. **중복 코드 패턴**
   - 선택 카드 렌더링 로직이 3번 반복됨
   - Step 구조가 인라인으로 정의되어 재사용 불가

5. **상태 관리 부재**
   - 로컬 상태만 사용 (useState)
   - 결과 데이터 영속성 없음 (새로고침 시 손실)

### 🎯 기회 (Opportunities)

1. **컴포넌트 분해**
   - StyleQuiz를 작은 컴포넌트들로 분리
   - SelectionCard, QuizStep 등 재사용 가능한 하위 컴포넌트 생성

2. **AI 통합**
   - 실제 Gemini API를 Result 페이지에 연결
   - 의류 추천, 아웃핏 제안 기능 활성화

3. **상태 관리 도입**
   - Context API 또는 Zustand로 전역 상태 관리
   - 결과 데이터 로컬 스토리지에 저장

4. **성능 최적화**
   - 이미지 lazy loading
   - 컴포넌트 code splitting
   - React.memo 활용

### 🚨 위협 (Threats)

1. **기술 부채 누적**
   - 미사용 컴포넌트가 계속 증가할 가능성
   - 리팩토링 없이 기능 추가 시 복잡도 증가

2. **확장성 제한**
   - 현재 구조로는 새로운 퀴즈 타입 추가 어려움
   - 다국어 지원 시 데이터 구조 변경 필요

---

## 5. 개선 제안

### 🎯 우선순위별 개선 과제

#### 🥇 High Priority (즉시 실행 권장)

##### 1. StyleQuiz 컴포넌트 분해

**현재 문제:**
```typescript
// style-quiz.tsx - 229 lines, too many responsibilities
export function StyleQuiz() {
  // State management (5 states)
  // Event handlers (3 functions)
  // Steps definition (150+ lines inline JSX)
  // Render logic
}
```

**개선 방안:**
```
src/components/quiz/
├── StyleQuiz.tsx (main orchestrator, ~80 lines)
├── QuizHeader.tsx (progress + title)
├── QuizFooter.tsx (navigation buttons)
├── steps/
│   ├── WelcomeStep.tsx
│   ├── StyleSelectionStep.tsx
│   ├── ColorSelectionStep.tsx
│   ├── InspirationSelectionStep.tsx
│   └── CompletionStep.tsx
└── shared/
    ├── SelectionCard.tsx (reusable)
    └── ColorPaletteCard.tsx (reusable)

src/hooks/
└── useQuizState.ts (state management logic)
```

**예상 효과:**
- ✅ 각 파일 50줄 이하로 축소 → 가독성 향상
- ✅ 컴포넌트 재사용성 증가
- ✅ 테스트 용이성 향상
- ✅ 병렬 개발 가능

##### 2. AI 기능 통합

**현재:**
```typescript
// Simulation only
await new Promise(resolve => setTimeout(resolve, 2000));
const analysisResult = `Based on your selections...`;
```

**개선:**
```typescript
// Real AI integration
import { generateStyleAnalysis } from "@/ai/flows/style-analysis";

const analysisResult = await generateStyleAnalysis({
  styles: selectedStyles,
  colors: selectedColors,
  inspirations: selectedInspirations,
});
```

**새로운 Flow 생성:**
```typescript
// src/ai/flows/style-analysis.ts
export async function generateStyleAnalysis(input: StyleAnalysisInput) {
  return styleAnalysisFlow(input);
}
```

##### 3. 미사용 컴포넌트 정리

**전략:**
```
Option A: 별도 브랜치로 이동
  git checkout -b archive/unused-components
  git mv src/components/ui/[unused-*.tsx] archive/
  
Option B: 주석 처리 + README에 명시
  // src/components/ui/README.md
  ## Available Components
  - ✅ button.tsx - In use
  - ⏳ dialog.tsx - Ready for use (not yet integrated)
  
Option C: 완전 삭제 (권장하지 않음)
  - 나중에 필요할 수 있음
```

**권장:** Option A (아카이브 브랜치)

#### 🥈 Medium Priority (다음 스프린트)

##### 4. 상태 관리 개선

```typescript
// src/context/QuizContext.tsx
export const QuizProvider = ({ children }) => {
  const [quizState, setQuizState] = useState({
    currentStep: 0,
    selections: {
      styles: [],
      colors: [],
      inspirations: [],
    },
    result: null,
  });

  // Persist to localStorage
  useEffect(() => {
    localStorage.setItem('quiz-state', JSON.stringify(quizState));
  }, [quizState]);

  return (
    <QuizContext.Provider value={{ quizState, setQuizState }}>
      {children}
    </QuizContext.Provider>
  );
};
```

##### 5. 타입 시스템 강화

```typescript
// src/types/quiz.ts
export interface QuizState {
  currentStep: number;
  selections: {
    styles: string[];
    colors: string[];
    inspirations: string[];
  };
  result: StyleAnalysisResult | null;
}

export interface QuizStep {
  id: string;
  title: string;
  description: string;
  component: React.ComponentType<QuizStepProps>;
  validation: (selections: string[]) => boolean;
}
```

#### 🥉 Low Priority (향후 고려)

##### 6. 성능 최적화

- Image lazy loading with Next.js Image
- React.memo for pure components
- useMemo for expensive calculations
- Code splitting for unused routes

##### 7. 테스트 추가

```typescript
// src/components/quiz/__tests__/SelectionCard.test.tsx
describe('SelectionCard', () => {
  it('should toggle selection on click', () => {
    // Test implementation
  });
});
```

##### 8. 접근성 개선

- ARIA labels 추가
- 키보드 네비게이션 지원
- Focus management
- Screen reader 최적화

---

## 📈 마이그레이션 로드맵

### Phase 1: 리팩토링 (Week 1-2)
- [ ] StyleQuiz 컴포넌트 분해
- [ ] 공통 컴포넌트 추출 (SelectionCard, etc.)
- [ ] useQuizState hook 생성
- [ ] 타입 정의 강화

### Phase 2: 기능 통합 (Week 3-4)
- [ ] AI flows 실제 연동
- [ ] 결과 저장 기능 (localStorage)
- [ ] 에러 처리 개선
- [ ] 로딩 상태 관리

### Phase 3: 최적화 (Week 5-6)
- [ ] 성능 최적화
- [ ] 접근성 개선
- [ ] 테스트 코드 작성
- [ ] 문서화 완료

---

## 🔍 권장 아키텍처 패턴

### 1. Compound Component Pattern
```typescript
<Quiz>
  <Quiz.Progress />
  <Quiz.Step>
    <Quiz.SelectionGrid>
      <Quiz.Card />
    </Quiz.SelectionGrid>
  </Quiz.Step>
  <Quiz.Navigation />
</Quiz>
```

### 2. Render Props Pattern
```typescript
<QuizStep
  render={({ selections, toggle }) => (
    <SelectionGrid items={items} onSelect={toggle} />
  )}
/>
```

### 3. Custom Hooks Pattern
```typescript
const { step, selections, nextStep, prevStep } = useQuizState();
const { loading, result, analyze } = useStyleAnalysis();
```

---

**문서 버전:** 1.0  
**최종 업데이트:** 2025-12-06  
**검토자:** Development Team






