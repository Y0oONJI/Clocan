# Archive - Unused UI Components

> **Created:** 2025-01-XX  
> **Purpose:** 미사용 shadcn/ui 컴포넌트 보관

---

## 📋 개요

이 폴더는 현재 프로젝트에서 사용되지 않는 shadcn/ui 컴포넌트들을 보관합니다.

### 통계

- **총 보관된 컴포넌트**: 21개
- **현재 사용 중인 컴포넌트**: 14개
- **사용률**: 40% (14/35)

---

## 📦 보관된 컴포넌트 목록

### 현재 미사용 (21개)

| 컴포넌트 | 설명 | 복원 가능 |
|---------|------|----------|
| `accordion` | 접을 수 있는 콘텐츠 섹션 | ✅ |
| `alert-dialog` | 중요한 메시지용 모달 다이얼로그 | ✅ |
| `alert` | 알림 메시지 | ✅ |
| `avatar` | 사용자 아바타 | ✅ |
| `calendar` | 달력 컴포넌트 | ✅ |
| `checkbox` | 체크박스 | ✅ |
| `collapsible` | 접기/펼치기 컴포넌트 | ✅ |
| `dialog` | 다이얼로그/모달 | ✅ |
| `dropdown-menu` | 드롭다운 메뉴 | ✅ |
| `form` | 폼 컴포넌트 | ✅ |
| `menubar` | 메뉴 바 | ✅ |
| `popover` | 팝오버 | ✅ |
| `radio-group` | 라디오 버튼 그룹 | ✅ |
| `scroll-area` | 스크롤 영역 | ✅ |
| `select` | 셀렉트 박스 | ✅ |
| `sidebar` | 사이드바 | ✅ |
| `slider` | 슬라이더 | ✅ |
| `switch` | 스위치 토글 | ✅ |
| `tabs` | 탭 컴포넌트 | ✅ |
| `textarea` | 텍스트 영역 | ✅ |
| `chart` | 차트 컴포넌트 | ✅ |

---

## 🔄 복원 방법

필요한 컴포넌트가 있다면 다음과 같이 복원할 수 있습니다:

### 1. 단일 컴포넌트 복원

```bash
# 예: dialog 컴포넌트 복원
cp archive/components/ui/dialog.tsx src/components/ui/dialog.tsx
```

### 2. 여러 컴포넌트 복원

```bash
# 예: form 관련 컴포넌트 복원
cp archive/components/ui/form.tsx src/components/ui/
cp archive/components/ui/input.tsx src/components/ui/
cp archive/components/ui/label.tsx src/components/ui/
```

### 3. 복원 후 확인

```bash
# TypeScript 타입 체크
npm run typecheck

# 빌드 확인
npm run build

# Lint 확인
npm run lint
```

---

## ✅ 현재 사용 중인 컴포넌트 (14개)

다음 컴포넌트들은 현재 프로젝트에서 사용 중이므로 **유지**됩니다:

- `badge` - 배지 (결과 페이지)
- `button` - 버튼 (전체)
- `card` - 카드 (전체)
- `carousel` - 캐러셀 (랜딩 페이지)
- `input` - 입력 필드 (사이드바)
- `label` - 라벨 (폼)
- `progress` - 진행 바 (퀴즈)
- `separator` - 구분선 (사이드바)
- `sheet` - 시트 (사이드바)
- `skeleton` - 스켈레톤 로딩
- `table` - 테이블 (어드민 대시보드)
- `toast` - 토스트 알림
- `toaster` - 토스터 컨테이너
- `tooltip` - 툴팁

---

## 📊 번들 크기 영향

### 예상 효과

- **번들 크기 감소**: 약 10-15% 감소 예상
- **파일 수 감소**: 21개 파일 제거
- **코드베이스 복잡도**: ⬇️⬇️ 감소
- **개발자 경험**: ⬆️ 파일 탐색 시 혼란 감소

---

## 🔍 사용 현황 확인

컴포넌트 사용 현황을 확인하려면:

```bash
# 특정 컴포넌트 사용 여부 확인
grep -r "from '@/components/ui/dialog'" src/

# 모든 UI 컴포넌트 사용 현황 확인
find src/components/ui -name "*.tsx" -exec basename {} .tsx \; | while read comp; do
  count=$(grep -r "from.*@/components/ui/$comp" src --include="*.tsx" --include="*.ts" | wc -l)
  echo "$comp: $count usages"
done
```

---

## 📝 참고 사항

- 모든 보관된 컴포넌트는 shadcn/ui 공식 컴포넌트입니다
- 필요 시 언제든지 복원 가능합니다
- 복원 후에는 해당 컴포넌트를 사용하는 코드를 작성해야 합니다
- 복원 시 의존성 확인이 필요할 수 있습니다 (예: `dialog`는 `button`에 의존)

---

## 🗂️ 폴더 구조

```
archive/
└── components/
    └── ui/
        ├── accordion.tsx
        ├── alert-dialog.tsx
        ├── alert.tsx
        ├── avatar.tsx
        ├── calendar.tsx
        ├── checkbox.tsx
        ├── collapsible.tsx
        ├── dialog.tsx
        ├── dropdown-menu.tsx
        ├── form.tsx
        ├── menubar.tsx
        ├── popover.tsx
        ├── radio-group.tsx
        ├── scroll-area.tsx
        ├── select.tsx
        ├── sidebar.tsx
        ├── slider.tsx
        ├── switch.tsx
        ├── tabs.tsx
        ├── textarea.tsx
        └── chart.tsx
```

---

**Last Updated:** 2025-01-XX
